#include<iostream>using namespace std;
const int size_ = 15;
int main(){ int tc; scanf("%d", &tc); for (int itr = 1; itr <= tc; ++itr) {  //이전라운드에 어떤걸로 끝났는지.  //0:Default,1:Spare,2:Strike  int chk_state[size_] = { 0 };  //점수 저장(라운드/횟수)  int score[size_][2] = { 0 };  int result[size_] = { 0 };  int strike_stack = 0;  //기본 라운드 10  for (int i = 0; i < 10; ++i)  {   //1라운드 점수 저장   int sum_temp = 0;   //1라운드당 2번 던짐   for (int j = 0; j < 2; ++j)   {    int input_;    scanf("%d", &input_);    sum_temp += input_;    score[i][j] = input_;    if (sum_temp == 10)    {     //Strike     if (j == 0) chk_state[i] = 2;     //Spare     else chk_state[i] = 1;     //last turn     if (i == 9)     {      //Spare      if (chk_state[i] == 1)      {       scanf("%d", &input_);       score[i + 1][0] = input_;      }      //Strike      else if (chk_state[i] == 2)      {       scanf("%d", &input_);       score[i + 1][0] = input_;       //Strike       if (input_ == 10)       {        chk_state[i + 1] = 2;        scanf("%d", &input_);        score[i + 2][0] = input_;       }       else       {        scanf("%d", &input_);        score[i + 1][1] = input_;       }      }     }     break;    }   }  }  //Calc Score  for (int i = 0; i < 10; ++i)  {   //현재 값 초기화   result[i] = score[i][0] + score[i][1];   //Spare   if (chk_state[i] == 1)   {    result[i] += score[i + 1][0];   }   //Strike   else if (chk_state[i] == 2)   {    //다음이 스트라이크일 경우    if (chk_state[i + 1] == 2) result[i] += (score[i + 1][0] + score[i + 2][0]);    //다음이 스트라이크가 아닐 경우    else result[i] += (score[i + 1][0] + score[i + 1][1]);   }  }  int total_ = 0;  for (int i = 0; i < 10; ++i)  {   //printf("#%d %d Round Score = %d\n", itr, i + 1, result[i]);   total_ += result[i];  }  printf("#%d Total Score = %d\n", itr, total_); } return 0;}
